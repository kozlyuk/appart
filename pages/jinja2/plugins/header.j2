<header id="header-wrap">
    <nav class="navbar navbar-expand-lg fixed-top scrolling-navbar indigo">
        <div class="container">
            <div class="navbar-header">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#main-navbar"
                    aria-controls="main-navbar" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    <span class="icon-menu"></span>
                    <span class="icon-menu"></span>
                    <span class="icon-menu"></span>
                </button>
                <a href="{{ url('house_main') }}" class="navbar-brand"><img style="height: 42px; width: auto;" src="{{ static ('global/logo.svg') }}" alt=""></a>
            </div>
            <div class="collapse navbar-collapse" id="main-navbar">
                <ul class="navbar-nav mr-auto w-100 justify-content-end clearfix">
                    <li class="nav-item {% if request.resolver_match.url_name == 'house_main' %}active{% endif %}"><a class="nav-link" href="{{ url('house_main') }}">{{ _('House') }}</a></li>
                    <li class="nav-item {% if request.resolver_match.url_name == 'company_main' %}active{% endif %}"><a class="nav-link" href="{{ url('company_main') }}">{{ _('Company') }}</a></li>
                    {# <li class="nav-item"><a class="nav-link" href="">{{ _('Profile') }}</a></li> #}
                </ul>
                <div class="btn-sing float-right">
                {% if request.user.is_authenticated %}
                    <a class="btn btn-border" href="{{ url('logout') }}">{{ _('Log out') }}</a>
                {% else %}
                    <a class="btn btn-border" href="{{ url('login') }}">{{ _('Log in') }}</a>
                {% endif %}
                </div>
            </div>
        </div>
        <ul class="mobile-menu navbar-nav">
            <li><a class="page-scroll" href="{{ url('house_main') }}">{{ _('House') }}</a></li>
            <li><a class="page-scroll" href="{{ url('company_main') }}">{{ _('Company') }}</a></li>
            <li><a class="page-scroll" href="{{ url('personal_cabinet') }}">{{ _('Personal cabinet') }}</a></li>s
            {# <li><a class="page-scroll" href="">{{ _('Profile') }}</a></li> #}
        </ul>
    </nav>
</header>